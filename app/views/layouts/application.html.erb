<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>MoneyPotter</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag "application" %>
    <%= stylesheet_pack_tag "styles" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
      <a class="navbar-brand" href="#">Money Potter</a>
    </nav>
    <div class="content">
      <% %w(danger success warning info).each do |type| %>
        <% if flash[type].present? %>
          <div class="flash flash-<%= type %>" role="alert">
            This is a test.
          </div>
        <% end %>
      <% end %>
      <main role="main" class="container">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
